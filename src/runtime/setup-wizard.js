import { applyWrites, planWrites } from "../file-writer.js";

export async function buildSetupWizard(root, { dryRun = true, output = ".product-ops/setup.html" } = {}) {
  const html = `<!doctype html><html lang="fa" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>راه‌اندازی عملیات محصول</title><style>body{font-family:Tahoma,Arial,sans-serif;max-width:760px;margin:40px auto;padding:20px;background:#f6f7fb;color:#172033}form{background:#fff;padding:24px;border-radius:14px;border:1px solid #ddd}label{display:block;margin:16px 0 6px}input,textarea,select{box-sizing:border-box;width:100%;padding:10px;border:1px solid #bbb;border-radius:8px}button{margin-top:20px;padding:12px 20px;border:0;border-radius:8px;background:#175cd3;color:#fff;cursor:pointer}</style></head><body><h1>راه‌انداز عملیات محصول</h1><p>این فرم فقط یک فایل پاسخ محلی می‌سازد و هیچ داده‌ای را ارسال نمی‌کند.</p><form id="setup"><label>نام محصول</label><input name="name" required><label>چشم‌انداز</label><textarea name="vision" required></textarea><label>کاربران هدف، جداشده با ویرگول</label><input name="targetUsers" required><label>شناسه مسئول انسانی</label><input name="humanAuthorityActorId" value="human-product-owner" required><label>محیط‌ها، جداشده با ویرگول</label><input name="environments" value="local,test,staging" required><button>دریافت فایل پیکربندی</button></form><script>document.getElementById('setup').addEventListener('submit',event=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.target));data.targetUsers=data.targetUsers.split(',').map(x=>x.trim()).filter(Boolean);data.environments=data.environments.split(',').map(x=>x.trim()).filter(Boolean);const blob=new Blob([JSON.stringify(data,null,2)+'\\n'],{type:'application/json'});const link=document.createElement('a');link.href=URL.createObjectURL(blob);link.download='product-ops-answers.json';link.click();URL.revokeObjectURL(link.href)});</script></body></html>\n`;
  const operations = await planWrites(root, new Map([[output, html]]), { force: true });
  if (!dryRun) await applyWrites(root, operations);
  return { dryRun, output, bytes: Buffer.byteLength(html) };
}
